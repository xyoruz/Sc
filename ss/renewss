#!/bin/bash
# ============================================================
# renewss - Renew Shadowsocks Account
# Lokasi: /usr/local/sbin/renewss
# ============================================================

BASE="/usr/local/sbin/xyr"
source "$BASE/lib/config.sh"
source "$BASE/lib/common.sh"
source "$BASE/lib/validate.sh"
source "$BASE/lib/xray-helper.sh"

require_root

print_header "RENEW SHADOWSOCKS ACCOUNT"

if [[ -f "$DB_SS" ]]; then
    printf "%-20s %s\n" "USERNAME" "EXPIRED"
    print_line
    awk -v p="$PREFIX_SS" '$1==p {printf "%-20s %s\n", $2, $3}' "$DB_SS"
    print_line
fi
echo ""

while :; do
    read -rp "Username: " USER
    if ! grep -qw "$USER" "$XRAY_CONF" 2>/dev/null; then
        print_error "User tidak ditemukan"; continue
    fi
    break
done

ask_number DAYS "Tambah berapa hari" 1 3650 30

old_exp=$(grep -m1 "^${PREFIX_SS} ${USER} " "$XRAY_CONF" | cut -d ' ' -f 3)
old_exp_ts=$(date -d "$old_exp" +%s 2>/dev/null || echo 0)
now_ts=$(date +%s)

if [[ "$old_exp_ts" -le "$now_ts" ]]; then
    base_ts=$now_ts
else
    base_ts=$old_exp_ts
fi

new_exp_ts=$((base_ts + DAYS * 86400))
new_exp_date=$(date -d "@$new_exp_ts" +"%Y-%m-%d")
new_exp_display=$(date -d "@$new_exp_ts" +"%d %b, %Y")

xray_backup_config

sed -i "s|^${PREFIX_SS} ${USER} [0-9-]*|${PREFIX_SS} ${USER} ${new_exp_date}|" "$XRAY_CONF"

if ! xray_apply; then
    print_error "Gagal update. Rollback."
    back_to_menu; exit 1
fi

if [[ -f "$DB_SS" ]]; then
    old_line=$(grep -E "^${PREFIX_SS} ${USER} " "$DB_SS")
    uuid=$(echo "$old_line" | awk '{print $4}')
    quota=$(echo "$old_line" | awk '{print $5}')
    ipl=$(echo "$old_line" | awk '{print $6}')
    sed -i "/^${PREFIX_SS} ${USER} /d" "$DB_SS"
    echo "${PREFIX_SS} ${USER} ${new_exp_date} ${uuid} ${quota} ${ipl}" >> "$DB_SS"
fi

log_info "Shadowsocks renewed: $USER -> $new_exp_date"

print_ok "User $USER diperpanjang sampai $new_exp_display"
back_to_menu